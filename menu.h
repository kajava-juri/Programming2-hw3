#ifndef MENU_H
#define MENU_H

/**
 * @brief Displays the main menu options to the user.
 */
void DisplayMenu();

/**
 * @brief Gets the user's menu selection input.
 * @returns The menu option selected by the user as an integer.
 */
int GetMenuSelection();


#endif // MENU_H