#ifndef MAIN_H
#define MAIN_H

/**
 * @brief Creates a duplicate of a string by allocating new memory.
 * @param src The source string to duplicate.
 * @returns Pointer to the newly allocated duplicate string, or NULL on failure.
 */
char *strdup(const char *src);

#endif // MAIN_H